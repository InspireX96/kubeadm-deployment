apiVersion: v1
kind: Service
metadata:
  name: kube-dashboard

spec:
  type: NodePort
  ports:
  - nodePort: 32008
    port: 443
    protocol: TCP
    targetPort: 8443
  selector:
    app: kube-dashboard
